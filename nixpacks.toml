# Railway / Nixpacks build configuration
# Two-phase build: 1) Frontend (Node.js) 2) Backend (Python)

[phases.frontend]
nixPkgs = ["nodejs_20"]
cmds = [
    "cd presentation/dashboard && npm ci && npm run build",
]

[phases.setup]
nixPkgs = ["python312", "gcc"]

[start]
cmd = "bash start.sh"
