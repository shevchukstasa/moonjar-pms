# Moonjar PMS — Nixpacks configuration for Railway
# Python backend + Node.js frontend build

[phases.setup]
aptPkgs = ["postgresql-client", "nodejs", "npm"]

[phases.build]
cmds = [
    "cd presentation/dashboard && npm ci && npm run build && cd ../..",
    "pip install -r requirements.txt",
]
